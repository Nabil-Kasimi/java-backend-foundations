Http protoccol, is multiple rules that give a data shared between two device or more a meaning it's like

two people want to communicate, they need to agree on a language (vocabulary) and grammar (how words are ordered).
If they don't, they are just making meaningless sounds at each other.
HTTP (Hypertext Transfer Protocol) provides that exact shared dictionary for the web.

